# API リファレンス（プレースホルダー）

API仕様書は現在整備中です。以下の観点で情報を追加してください。

- 認証が必要なエンドポイントの一覧とスコープ
- `/api/` 以下のサーバーアクション・ルートハンドラの入出力
- エラーレスポンス形式とステータスコード規約
- クライアントSDK（必要に応じて `lib/` や `services/` の利用方法）

ドラフトが完成したら、このREADMEから個別ファイルへリンクする構造に変更します。
